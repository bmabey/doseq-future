(defproject doseq-future "1.0.0-SNAPSHOT"
  :description "example of how lazy-seqs are not released when realized in a future (when they are created prior to the future)"
  :dependencies [[org.clojure/clojure "1.2.0-RC2"]])
